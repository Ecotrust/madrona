lingcod.rest = {}

lingcod.rest.clients_for_document = function(document){
    // parse the document for atom links
    // find those with unique mm:models
    // return a new lingcod.rest.Client for each of them
}