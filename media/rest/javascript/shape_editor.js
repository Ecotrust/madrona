lingcod.rest.ShapeEditor = function(render_to){
}

lingcod.rest.ShapeEditor.prototype.add = function(kml_links){
    // Load all kml files, calling callback
}

lingcod.rest.ShapeEditor.prototype.kml_callback = function(args){
    // inspect kml, finding all unique models and form links
    // create new instance of lingcod.rest.Client for each
    // add to dictionary
    // add kml tree widget
    // for each tree, add ADD buttons
}